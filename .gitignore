# Kubernetes / kind
kubeconfig
*.kubeconfig
.kube/

# Local dev state
/tmp/
*.log
local/.console.pid
local/.config.json

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo
.mcp.json

# Secrets (belt-and-suspenders — nothing sensitive should live here anyway)
*.pem
*.key
.env
.env.local
secrets.yaml
!charts/*/secrets.yaml  # unless it's a template with placeholders
.gstack/
.vercel
.env*
